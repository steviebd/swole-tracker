import { setupDevPlatform } from '@cloudflare/next-on-pages/next-dev'

// note: this should be configured in your wrangler.toml file
// as the name of the main export of this file
export default setupDevPlatform()
